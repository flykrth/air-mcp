# Supabase Credentials (for database persistence in production)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# MCP Server Endpoint
# If set, the backend will communicate over SSE to this remote MCP Server.
# Otherwise, it falls back to executing the local node mcp-server.
MCP_SERVER_URL=https://air-mcp-6a5b16a4-tatva-amrita-university-amritapuri-campus.app.nitrocloud.ai/sse

# CORS Allowed Origin (defaults to Vercel production deployment)
FRONTEND_URL=https://air-mcp.vercel.app

# API Gateway Settings
PORT=8000
HOST=0.0.0.0
